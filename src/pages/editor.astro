---
// src/pages/index.astro
import Layout from '../layouts/Layout.astro';
import AppShell from '../components/AppShell.astro';
import Header from '../components/Header.svelte'; // 👈 副檔名變了
import MediaSidebar from '../components/MediaSidebar.astro';
import VideoPlayer from '../components/VideoPlayer.svelte'; 
import Timeline from '../components/Timeline.svelte';   // ✅ 改用這個
import PropertiesPanel from '../components/PropertiesPanel.svelte'; // 👈 副檔名變了
import ProjectSaver from '../components/ProjectSaver.svelte';
---

<Layout>
  <!-- 使用 AppShell 來管理佈局 -->
 <AppShell cleanMode={true}>
        <!-- 🔥🔥🔥 新增這一行：給 Bing 和 Google 看的隱藏標題 🔥🔥🔥 -->
    <!-- sr-only 讓它在視覺上消失，但 SEO 抓得到 -->
    <h1 class="sr-only">FastVideoCutter Editor - Edit Video Online for Free</h1>

      <ProjectSaver client:only="svelte" slot="header" /> 
    <!-- 1. 頂部 Header -->
    <Header slot="header" client:load />

    <!-- 2. 左側 Sidebar -->
    <MediaSidebar slot="left-sidebar" />

    <!-- 3. 中間區域 (這裡我們直接放入兩個元件，因為 AppShell 的 main 插槽是 flex-col) -->
    <div slot="main" class="flex flex-col h-full">
       <VideoPlayer client:load />
      <Timeline client:load />
    </div>

     <!-- 👇 加上 client:load 以啟用互動 -->
    <PropertiesPanel slot="right-sidebar" client:load />

  </AppShell>
</Layout>